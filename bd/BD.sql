Create Table Notas (
Id Int(11) NOT NULL AUTO_INCREMENT,
Alumno Varchar(65) NOT NULL,
TA Int NOT NULL,
PC Int NOT NULL,
PE Int NOT NULL,
Promedio Float NOT NULL,
Obser Varchar(65) NOT NULL,
Fecha Varchar (10) NOT NULL,
PRIMARY KEY (Id )
);